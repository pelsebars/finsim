# PostgreSQL connection string
# Local: postgresql://user:password@localhost:5432/finsim
# Production: use Railway connection string
DATABASE_URL=postgresql://user:password@localhost:5432/finsim

# Secret used to sign JWT tokens — must be a long random string
JWT_SECRET=change-this-to-a-long-random-secret
